export interface Config {

  // controller app url
  _app:string;

  // layers definitions
  // number of layers
  nLayers:number;
  // layer delta - distance between adjacent layers
  layerDelta:number;
}

